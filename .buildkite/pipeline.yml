steps:
    - name: ":golang:"
      plugins:
          - docker#v2.2.0:
              image: "docker:latest"
              workdir: /work
              volumes:
                - "paul-bot:/work"
                - "/var/run/docker.sock:/var/run/docker.sock"
      commands:
          - "docker build . -t paul-bot:latest"
